import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.verum.omnis',
  appName: 'Verum Omnis',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
