{
  "name": "verum-omnis-forensic-ai",
  "version": "5.2.7",
  "private": true,
  "description": "Stateless, constitutional forensic AI assistant.",
  "scripts": {
    "dev": "echo 'Run by opening index.html in a browser'",
    "build": "echo 'No build step required'",
    "android": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}